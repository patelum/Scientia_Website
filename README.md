# Scientia Clinical Research Website

A modern, responsive, SEO-optimised landing website for Scientia Clinical Research.

## Scripts

<<<<<<< codex/build-seo-optimized-website-for-scientia-tn7s07
- `npm run dev` starts a local Python static file server.
- `npm run build` creates a production build in `dist/`.
- `npm run preview` serves the production build locally.

## GitHub Pages

Publish from the repository root. The CSS and JavaScript are root-level files (`styles.css` and `main.js`) so they load correctly from GitHub Pages project URLs.
=======
- `npm run dev` starts a local Vite development server.
- `npm run build` creates a production build in `dist/`.
- `npm run preview` serves the production build locally.
>>>>>>> main
