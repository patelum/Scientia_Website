# Scientia Clinical Research Website

A modern, responsive, SEO-optimised landing website for Scientia Clinical Research.

## Scripts

- `npm run dev` starts a local Vite development server.
- `npm run build` creates a production build in `dist/`.
- `npm run preview` serves the production build locally.
