{
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css main.js robots.txt sitemap.xml dist/",
    "dev": "python3 -m http.server 4173",
    "preview": "python3 -m http.server 4173 -d dist"
  },
  "dependencies": {},
  "devDependencies": {}
}
